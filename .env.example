DATABASE_URL="file:./dev.db"

# Required for real AI-assisted diagnosis, scoring, and Socratic repair generation.
# When unset, the app uses deterministic local fallback scoring for offline development.
OPENAI_API_KEY=""
OPENAI_MODEL="gpt-5.4-mini"
